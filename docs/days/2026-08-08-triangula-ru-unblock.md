# 2026-08-08 — Triangula: починен гео-блок RU (отчёт не генерировался для России)

## Симптом
Сергей: «не генерируется отчёт на triangula.app после фото и 12 вопросов».
Фото и вопросы проходили, но после нажатия кнопки — ошибка/пусто.

## Диагностика
- `/api/reading` с сервера (Hetzner, Финляндия) → 200, полный отчёт. Значит бэкенд жив.
- Полный флоу в браузере (фото из галереи + 12 ответов) → результат «Pure Partner» 73% — фронтенд рабочий.
- **Корень: `src/worker.js` строка 204 — `BLOCKED_COUNTRIES = ['IL', 'RU']` с самого первого коммита (Initial commit f27a12d).**
- Cloudflare определяет страну по IP клиента → российский IP → 451 `region_unavailable` → «Triangula is not yet available in your region» → отчёт не генерируется.
- В вики/истории — никаких записей, что блок RU был осознанным решением. Просто остаток раннего прототипа.

## Фикс
- `BLOCKED_COUNTRIES = ['IL']` (RU убран, IL оставлен — причина его там неизвестна).
- Commit `571ac7d`, push → CI deploy success за 41с (run 31263090745).
- Проверено после деплоя: полный флоу на живом сайте — 200, «Pure Partner» 73%.

## Выводы
- Проверять geofence при жалобах «не работает с моего телефона» — RU в блок-листе сразу бьёт по самому владельцу.
- Сергею: проверить с телефона (без VPN). Если снова 451 — смотреть CF-IPCountry в логах.
- IL в блок-листе остался — уточнить у Сергея, зачем он там, или убрать аналогично.
